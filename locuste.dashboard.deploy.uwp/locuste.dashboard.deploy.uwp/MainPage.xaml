<Page
    x:Class="locuste.dashboard.deploy.uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Mettre une Frame ici -->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0">
            <AppBarButton x:Name="UpdaterMenuBtn" Icon="Sync" Label="Mises à jours" Click="UpdaterMenuBtn_Click"></AppBarButton>
            <AppBarButton x:Name="AddDeviceBtn" Icon="Add" Label="Ajouter des appareils" Click="AddDeviceBtn_Click"></AppBarButton>
            <AppBarButton Icon="Setting" Label="Réglage"></AppBarButton>
        </CommandBar>

        <Frame x:Name="TopFrame" Grid.Row="1">

        </Frame>
    </Grid>
</Page>
